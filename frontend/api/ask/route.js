/* DISABLED for Hackathon - Frontend Next.js API route bypassed.
All queries now route through services/api.js → backend FastAPI :8000/ask for:
- Proper RAG (TF-IDF embeddings)
- Chat history/multi-turn (TC-03) 
- Source citations in responses (TC req)
- Out-of-scope handling (TC-05)
- Detailed structured answers (TC-01/02/04)

Original static keyword search + no-citation Groq removed.
Backend handles all LLM/retrieval.
 */
